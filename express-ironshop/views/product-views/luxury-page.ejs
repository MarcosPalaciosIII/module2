<h2> Top 20 Luxury Product </h2>

<ol>
  <% listOfExpensiveProducts.forEach((oneProduct) => { %>
    <li>
      <h3> <%= oneProduct.name %> </h3>
      <p> <%= oneProduct.price %> </p>
      <img src="<%= oneProduct.imageURL %>" />
      <br />

      <a href="/products/ <%= oneProduct._id %>">
        View Details
      </a>
    </li>
  <% }) %>
</ol>
